{
  "name": "driveshare",
  "private": true,
  "scripts": {
    "start": "npm run build --workspace client && node server.js",
    "test": "node --test"
  },
  "workspaces": ["client"],
  "dependencies": {
    "express": "^4.21.2"
  }
}